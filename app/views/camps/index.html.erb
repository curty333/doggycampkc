<p id="notice"><%= notice %></p>

<div class ="jumbotron">
  <h2>DoggieCampKC is the best way to find reviews on local housing and kennel options in the KC area.</h2>
  <h3>Explore the most favorited doggie camps in your community.
  <br>Leave reviews and ratings for your fellow dog lovers.</h3>
</div>

<table class="table table-hover table-condensed">
  <thead>
    <tr>
      <th>Name</th>
      <th>Address</th>
      <th>Phone</th>
      <th>Website</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @camps.each do |camp| %>
      <tr>
        <td><%= camp.name %></td>
        <td><%= camp.address %></td>
        <td><%= camp.phone %></td>
        <td><%= link_to camp.website, camp.website %></td>
        <td><%= link_to 'Show', camp %></td>
        <td><%= link_to 'Edit', edit_camp_path(camp) %></td>
        <td><%= link_to 'Destroy', camp, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Camp', new_camp_path %>
